# Server Configuration — copy to .env and set values. Never commit .env or real keys.
PORT=5000
NODE_ENV=development

# MongoDB (required) — use a local URI or MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/naomedical

# AI Summary (optional) — get a free key at https://console.groq.com/
# GROQ_API_KEY=
